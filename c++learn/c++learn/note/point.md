# 指针
申明指针变量之后，在没有赋值之前，里面存放的是乱码
指针存放的是地址；
*是解引用运算符，可以得到地址中存放的值，&是取地址运算符，拿到变量的地址。
那么，C++中有两种存储数据的方式：
- 正常声明变量，由编译器跟踪该变量；
- 声明指针变量，这个指针变量存储地址，访问的时候直接访问该地址
## 指针用于函数
地址传递：形参定义为指针，传参传变量地址
值传递：函数形参为普通变量
```cpp
	
```
